# Changelog

이 저장소(2026-2 고급알고리즘 과제 1 — 정렬 비교)의 변경 기록. 형식은
[Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르고,
버전은 [유의적 버전](https://semver.org/lang/ko/)을 따른다.

## [1.0] - 2026-09-18

첫 릴리스. 삽입 · 버블 · 블록 정렬을 하나의 공통 인터페이스로 묶고, 같은
잣대로 속도 · 메모리 · 안정성을 잰 결과를 보고서로 정리했다.

### Added

- **공통 인터페이스** (`src/sort.h`). 함수 포인터를 담은 구조체
  `SortAlgorithm`과 구현 표 `SORT_ALGORITHMS`로 정렬 셋을 묶었다. 부르는 쪽은
  정렬 이름을 적지 않고 표만 훑는다. 비교는 `qsort` 규약의 함수 포인터로 받아
  원소 타입에서 자유롭고, 그 덕분에 안정성을 실측할 수 있다.
- **정렬 세 가지**: `insertionSort` · `bubbleSort` · `blockSort`. 파일 하나에
  알고리즘 하나씩 두고, 구현끼리만 쓰는 도구는 `src/sortctx.h`로 갈랐다.
  블록 정렬은 회전(뒤집기 세 번)으로 제자리 병합해 `O(1)` 메모리를 지키는
  안정 정렬이고, 블록 하나를 정리할 때 삽입 정렬을 재사용한다.
- **측정 도구** (`src/bench.c`). 시간 · 비교 · 이동 · 추가 메모리 · 재귀 깊이를
  재고, `(key, tag)` 원소로 안정성을 실측한다. 정렬은 자기가 측정당하는 줄
  모른다. 입력은 무작위 · 정렬됨 · 역순 · 중복많음 네 모양이고 씨앗을 고정한다.
- **유닛 테스트 42개** (`tests/test_sort.c`). 구현 표를 훑으므로 정렬을 하나 더
  넣으면 그 순간부터 같은 검사를 받는다. 기본 케이스 · `qsort` 대조 ·
  `n = 0..200` 전수 · 안정성이 구현 표의 `stable` 값과 맞는지까지 본다.
  외부 프레임워크를 쓰지 않는다.
- **비교 표와 CSV 출력** (`src/main.c`). 무엇을 잴지는 `SPECS` 한 곳에만 적고,
  표로 찍을지 CSV로 찍을지는 함수 포인터로 갈아 끼운다. `--csv`로 측정값을,
  `--blocks`로 블록 크기를 바꿔 가며 잰 값을 찍는다.
- **그래프** (`tools/svgchart.py` · `tools/plot.py`). 외부 라이브러리 없이 표준
  모듈만으로 SVG를 직접 찍는다. 같은 자료를 선형 축과 로그 축으로 각각 그린다.
  `make charts`로 다시 만든다.
- **보고서** (`report/REPORT.md`). 코드 보고서 · 알고리즘 상세 · 실험 결과의 세
  부분이다. 측정값 원본(`results.csv` · `block-size.csv`)과 그래프를 같이 둔다.
- **인터랙티브 데모** (`report/block_sort.html`). 블록 정렬이 도는 과정을 한
  단계씩 따라간다. 브라우저로 열기만 하면 되고 설치할 것은 없다.

### Changed

- **블록 크기를 `ceil(√n)`에서 32 상수로 바꿨다.** WikiSort의 `√n`은 내부 버퍼의
  크기이지 초기 블록의 길이가 아니어서, 근거 없이 빌려온 값이었다. 재 보니
  최적 블록 크기는 `n`을 따라가지 않는 작은 상수다 — `n = 64,000`에서
  `ceil(√n)` = 253은 32보다 비교를 2.6배 더 썼다.
- **Python 구현을 뺐다.** 이 과제는 C만 낸다.

### Notes

- 검증은 유닛 테스트 외에 **변이 테스트**와 **ASan · UBSan**으로 보강했다.
  삽입 정렬의 `>`를 `>=`로 바꾸면 검사 6개가(삽입 3 + 블록 3, 같은 함수를
  쓴다), 제자리 병합의 종료 조건을 뒤집으면 `assert`가 걸린다.
- `O(1)` 메모리에 **스택은 세지 않았다.** 제자리 병합의 재귀가 `O(log n)`
  깊이다. 자세한 것은 보고서 3.3절에 적었다.
